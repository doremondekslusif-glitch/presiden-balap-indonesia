export class Hud {constructor(){this.$=s=>document.querySelector(s);this.map=this.$('#map');this.ctx=this.map.getContext('2d');} update(race){const p=race.karts[0],rank=race.rankings().indexOf(p)+1;this.$('#position').innerHTML=`${rank}<small>/4</small>`;this.$('#lap').textContent=`LAP ${Math.min(p.lap+1,race.circuit.laps)}/${race.circuit.laps}`;this.$('#speed').textContent=String(Math.round(Math.abs(p.speed)*8)).padStart(3,'0');this.$('#boost-fill').style.width=p.boost+'%';let t=race.elapsed,m=Math.floor(t/60),s=Math.floor(t%60),ms=Math.floor((t%1)*1000);this.$('#timer').textContent=`${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}.${String(ms).padStart(3,'0')}`;this.drawMap(race,p);}
 drawMap(race,p){const c=this.ctx,w=this.map.width,h=this.map.height;c.clearRect(0,0,w,h);const pts=race.track.points;c.strokeStyle='#f4c542';c.lineWidth=5;c.beginPath();pts.forEach((q,i)=>{let x=w/2+q.x*1.35,y=h/2+q.z*1.35;i?c.lineTo(x,y):c.moveTo(x,y)});c.closePath();c.stroke();race.karts.forEach(k=>{c.fillStyle=k===p?'#e63946':'#e9eef5';c.beginPath();c.arc(w/2+k.mesh.position.x*1.35,h/2+k.mesh.position.z*1.35,k===p?5:3,0,7);c.fill()})}}
